spring:
  application:
    name: hrmanager
  #########################
  #       DATABASE
  #########################
  datasource:
    url: "jdbc:postgresql://localhost:5432/hrmanager_db"
    username: yandexgo
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080